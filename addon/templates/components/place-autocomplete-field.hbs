{{#if hasBlock}}
  {{yield}}
{{else}}
  {{input value=value
          classBinding="inputClass"
          tabindex=tabindex
          disabled=disabled
          focus-out='focusOut'
          id=id
          placeholder=placeholder}}
{{/if}}